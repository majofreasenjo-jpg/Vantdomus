NEXT_PUBLIC_API_BASE=http://127.0.0.1:8001
VANTDOMUS_WEB_PROXY_MAX_BODY_BYTES=10485760
VANTDOMUS_WEB_PUBLIC_PROXY_MAX_BODY_BYTES=1048576
# Local demo only. Leave empty for staging/production; the web preflight blocks public static access tokens.
NEXT_PUBLIC_ACCESS_TOKEN=PASTE_JWT
# Local demo only. Leave empty for staging/production; tenant selection must happen after login.
NEXT_PUBLIC_DEFAULT_HOUSEHOLD_ID=PASTE_HOUSEHOLD_ID
