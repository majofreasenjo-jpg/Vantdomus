name: DOMI G5 R3 G4 Primary Certification

on:
  push:
    branches:
      - domi-g5-r3-autobiographical-history
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  g4-primary-cert:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Verify frozen R3 primary evaluator and expectation matrix
        working-directory: apps/web
        run: node --test tests/domi-g5-r3-primary-evaluator-freeze.test.mjs
